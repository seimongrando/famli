# 🏗️ Arquitetura do Sistema

Este documento descreve a arquitetura técnica do Famli, incluindo decisões de design, fluxos de dados e responsabilidades de cada componente.

---

## 📋 Índice

1. [Visão Geral](#visão-geral)
2. [Tecnologias](#tecnologias)
3. [Backend](#backend)
4. [Frontend](#frontend)
5. [Fluxos de Dados](#fluxos-de-dados)
6. [Segurança](#segurança)
7. [Escalabilidade](#escalabilidade)

---

## Visão Geral

O Famli é uma aplicação monolítica moderna com separação clara entre frontend e backend, mas distribuída como um único pacote para simplificar o deploy.

### Diagrama de Alto Nível

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTES                                │
├─────────────┬─────────────┬─────────────┬─────────────┬────────┤
│   Browser   │     PWA     │   Android   │     iOS     │WhatsApp│
│   (Vue 3)   │ (Vite PWA)  │ (Capacitor) │ (Capacitor) │(Twilio)│
└──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┴───┬────┘
       │             │             │             │          │
       └─────────────┴──────┬──────┴─────────────┘          │
                            │                               │
                    ┌───────▼───────┐               ┌───────▼───────┐
                    │   HTTPS/443   │               │ Webhook/8080  │
                    └───────┬───────┘               └───────┬───────┘
                            │                               │
┌───────────────────────────▼───────────────────────────────▼─────┐
│                     BACKEND (Go + Chi)                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    MIDDLEWARES                           │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐│   │
│  │  │ Request │ │  CORS   │ │Security │ │  Rate Limiter   ││   │
│  │  │   ID    │ │ Handler │ │ Headers │ │ (por IP/rota)   ││   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      HANDLERS                            │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐│   │
│  │  │  Auth   │ │   Box   │ │Guardian │ │    WhatsApp     ││   │
│  │  │ Handler │ │ Handler │ │ Handler │ │    Handler      ││   │
│  │  └────┬────┘ └────┬────┘ └────┬────┘ └────────┬────────┘│   │
│  │       │           │           │               │          │   │
│  │  ┌────▼───────────▼───────────▼───────────────▼────────┐│   │
│  │  │                   SECURITY                           ││   │
│  │  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────────┐││   │
│  │  │  │Validate│ │ Crypto │ │ Audit  │ │  Rate Limit    │││   │
│  │  │  │  Input │ │AES-256 │ │ Logger │ │   Handler      │││   │
│  │  │  └────────┘ └────────┘ └────────┘ └────────────────┘││   │
│  │  └─────────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                     STORAGE                              │   │
│  │  ┌─────────────────────────────────────────────────────┐│   │
│  │  │              MemoryStore (MVP)                       ││   │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────┐ ││   │
│  │  │  │  Users  │ │  Items  │ │Guardians│ │  Settings │ ││   │
│  │  │  └─────────┘ └─────────┘ └─────────┘ └───────────┘ ││   │
│  │  └─────────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              STATIC FILE SERVER                          │   │
│  │              (frontend/dist/)                            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Tecnologias

### Stack Principal

| Camada | Tecnologia | Versão | Justificativa |
|--------|------------|--------|---------------|
| **Backend** | Go | 1.21+ | Performance, simplicidade, tipos |
| **Router** | Chi | 5.x | Leve, idiomático, middlewares |
| **Frontend** | Vue 3 | 3.4+ | Composition API, reatividade |
| **Build** | Vite | 5.x | HMR rápido, ESM nativo |
| **State** | Pinia | 2.x | Type-safe, devtools |
| **i18n** | vue-i18n | 9.x | Suporte completo |
| **PWA** | vite-pwa | 0.20+ | Service worker automático |
| **Mobile** | Capacitor | 6.x | Acesso nativo, web first |

### Dependências de Segurança

| Pacote | Uso |
|--------|-----|
| `golang.org/x/crypto/bcrypt` | Hashing de senhas |
| `golang.org/x/crypto/argon2` | Derivação de chaves |
| `github.com/golang-jwt/jwt/v5` | Tokens JWT |

---

## Backend

### Estrutura de Pacotes

```
backend/
├── main.go                    # Entry point, configuração de rotas
└── internal/                  # Código privado (não exportável)
    ├── auth/
    │   ├── handler.go         # Endpoints de autenticação
    │   └── middleware.go      # JWT middleware
    ├── box/
    │   └── handler.go         # CRUD de itens
    ├── guardian/
    │   └── handler.go         # CRUD de guardiões
    ├── guide/
    │   └── handler.go         # Cards do guia
    ├── i18n/
    │   └── i18n.go            # Traduções do backend
    ├── security/
    │   ├── audit.go           # Logging de segurança
    │   ├── crypto.go          # AES-256-GCM, Argon2
    │   ├── headers.go         # Security headers
    │   ├── ratelimit.go       # Rate limiting
    │   └── validation.go      # Input validation
    ├── settings/
    │   └── handler.go         # Configurações do usuário
    ├── storage/
    │   ├── models.go          # Modelos de dados
    │   └── memory.go          # Storage em memória (MVP)
    └── whatsapp/
        ├── handler.go         # Webhook endpoints
        ├── models.go          # Modelos de mensagem
        ├── service.go         # Lógica de processamento
        └── twilio.go          # Cliente Twilio
```

### Responsabilidades por Pacote

#### `auth/`
- **handler.go**: Registro, login, logout, sessão
  - Validação de credenciais
  - Geração de JWT
  - Proteção contra timing attacks
  - Rate limiting de login

- **middleware.go**: Proteção de rotas
  - Extração de JWT do cookie
  - Validação de assinatura e expiração
  - Injeção de userID no contexto

#### `box/`
- **handler.go**: CRUD de itens da Caixa Famli
  - Validação e sanitização de inputs
  - Auditoria de acessos
  - Isolamento por usuário

#### `guardian/`
- **handler.go**: CRUD de pessoas de confiança
  - Validação de telefone/email
  - Relacionamentos pré-definidos

#### `guide/`
- **handler.go**: Guia Famli
  - Listagem de cards
  - Tracking de progresso

#### `security/`
- **audit.go**: Logging de eventos de segurança
  - Detecção de anomalias
  - Alertas automáticos

- **crypto.go**: Criptografia
  - AES-256-GCM para dados sensíveis
  - Argon2id para derivação de chaves

- **headers.go**: Headers HTTP
  - CSP, HSTS, X-Frame-Options
  - Configurações por ambiente

- **ratelimit.go**: Rate limiting
  - Por IP e por endpoint
  - Bloqueio progressivo

- **validation.go**: Validação
  - Email, senha, telefone, URL
  - Sanitização de HTML

#### `storage/`
- **models.go**: Definição de entidades
  - User, BoxItem, Guardian, Settings

- **memory.go**: Armazenamento em memória
  - Thread-safe com mutex
  - CRUD completo

#### `whatsapp/`
- **handler.go**: Endpoints do webhook
  - Recebimento de mensagens
  - Vinculação de conta

- **service.go**: Processamento de mensagens
  - Interpretação de comandos
  - Criação de itens
  - Gerenciamento de sessão

- **twilio.go**: Cliente Twilio
  - Envio de mensagens
  - Validação de webhook

---

## Frontend

### Estrutura de Arquivos

```
frontend/src/
├── main.js                    # Entry point, plugins
├── App.vue                    # Componente raiz
├── components/                # Componentes reutilizáveis
│   ├── AssistantChat.vue      # Chat do assistente
│   ├── BoxComposer.vue        # Formulário de criação
│   ├── BoxFeed.vue            # Lista de itens
│   ├── GuideCard.vue          # Card do guia
│   ├── LanguageSelector.vue   # Seletor de idioma
│   ├── PasswordStrength.vue   # Validação de senha
│   ├── PrivacyModal.vue       # Modal de privacidade
│   └── SettingsModal.vue      # Modal de configurações
├── pages/                     # Views/páginas
│   ├── LandingPage.vue        # Página inicial
│   ├── AuthPage.vue           # Login/registro
│   └── DashboardPage.vue      # Dashboard principal
├── stores/                    # Pinia stores
│   ├── auth.js                # Estado de autenticação
│   ├── box.js                 # Estado da caixa
│   └── guide.js               # Estado do guia
├── i18n/                      # Internacionalização
│   ├── index.js               # Configuração vue-i18n
│   └── locales/
│       ├── pt-BR.json         # Português
│       └── en.json            # Inglês
└── styles/
    └── main.css               # Estilos globais + design system
```

### Responsabilidades por Componente

#### Páginas (`pages/`)

| Página | Rota | Responsabilidade |
|--------|------|------------------|
| `LandingPage.vue` | `/` | Apresentação do produto |
| `AuthPage.vue` | `/entrar` | Login e registro |
| `DashboardPage.vue` | `/minha-caixa` | Dashboard principal |

#### Componentes (`components/`)

| Componente | Responsabilidade |
|------------|------------------|
| `AssistantChat.vue` | Interface do assistente IA |
| `BoxComposer.vue` | Formulário para criar itens |
| `BoxFeed.vue` | Lista e filtros de itens |
| `GuideCard.vue` | Card individual do guia |
| `LanguageSelector.vue` | Dropdown de idiomas |
| `PasswordStrength.vue` | Validação visual de senha |
| `PrivacyModal.vue` | Informações de privacidade |
| `SettingsModal.vue` | Configurações do usuário |

#### Stores (`stores/`)

| Store | Estado | Ações |
|-------|--------|-------|
| `auth.js` | user, isAuthenticated, loading, error | login, register, logout, checkSession |
| `box.js` | items, loading, error | fetchItems, createItem, updateItem, deleteItem |
| `guide.js` | cards, progress, loading | fetchCards, fetchProgress, markProgress |

---

## Fluxos de Dados

### Autenticação

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   Browser   │      │   Backend   │      │   Storage   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │
       │ POST /api/auth/login                    │
       │ {email, password}  │                    │
       ├───────────────────►│                    │
       │                    │ GetUserByEmail()   │
       │                    ├───────────────────►│
       │                    │◄───────────────────┤
       │                    │                    │
       │                    │ bcrypt.Compare()   │
       │                    │ jwt.Sign()         │
       │                    │                    │
       │ Set-Cookie: famli_session=JWT          │
       │◄───────────────────┤                    │
       │                    │                    │
       │ (subsequentes)     │                    │
       │ Cookie: famli_session                   │
       ├───────────────────►│                    │
       │                    │ JWT.Verify()       │
       │                    │ ctx.Set(userID)    │
       │                    │                    │
```

### Criação de Item

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  Frontend   │      │   Backend   │      │   Storage   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │
       │ POST /api/box/items                     │
       │ {type, title, content}                  │
       ├───────────────────►│                    │
       │                    │                    │
       │                    │ JWTMiddleware      │
       │                    │ GetUserID(ctx)     │
       │                    │                    │
       │                    │ Validate(payload)  │
       │                    │ Sanitize(payload)  │
       │                    │                    │
       │                    │ CreateBoxItem()    │
       │                    ├───────────────────►│
       │                    │◄───────────────────┤
       │                    │                    │
       │                    │ AuditLog(create)   │
       │                    │                    │
       │ 201 Created        │                    │
       │ {id, title, ...}   │                    │
       │◄───────────────────┤                    │
       │                    │                    │
```

### Integração WhatsApp

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  WhatsApp   │      │   Twilio    │      │   Backend   │      │   Storage   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │                    │
       │ Usuário envia msg  │                    │                    │
       ├───────────────────►│                    │                    │
       │                    │ POST /api/whatsapp/webhook              │
       │                    ├───────────────────►│                    │
       │                    │                    │                    │
       │                    │                    │ ParseMessage()     │
       │                    │                    │ GetSession(phone)  │
       │                    │                    │ ProcessMessage()   │
       │                    │                    │                    │
       │                    │                    │ [se vinculado]     │
       │                    │                    │ CreateBoxItem()    │
       │                    │                    ├───────────────────►│
       │                    │                    │◄───────────────────┤
       │                    │                    │                    │
       │                    │ TwiML Response     │                    │
       │                    │◄───────────────────┤                    │
       │◄───────────────────┤                    │                    │
       │                    │                    │                    │
```

---

## Segurança

### Camadas de Proteção

```
┌─────────────────────────────────────────────────────────────────┐
│                     INTERNET                                    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  FIREWALL / CDN / Load Balancer                                 │
│  - WAF (Web Application Firewall)                               │
│  - DDoS Protection                                              │
│  - SSL/TLS Termination                                          │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  RATE LIMITING (Aplicação)                                      │
│  - Por IP: 100 req/min geral, 5 req/min login                   │
│  - Bloqueio progressivo após falhas                             │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  SECURITY HEADERS                                               │
│  - CSP: Controle de recursos                                    │
│  - HSTS: Força HTTPS                                            │
│  - X-Frame-Options: Anti-clickjacking                           │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  AUTENTICAÇÃO                                                   │
│  - JWT em cookie HttpOnly                                       │
│  - Senhas: bcrypt (custo 10)                                    │
│  - Proteção timing attacks                                      │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  VALIDAÇÃO                                                      │
│  - Sanitização de inputs                                        │
│  - Limites de tamanho                                           │
│  - Detecção de injection                                        │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  CRIPTOGRAFIA                                                   │
│  - AES-256-GCM para dados sensíveis                             │
│  - Argon2id para derivação de chaves                            │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│  AUDITORIA                                                      │
│  - Logs estruturados (JSON)                                     │
│  - Eventos de segurança                                         │
│  - Alertas automáticos                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## Escalabilidade

### Atual (MVP)

```
┌───────────────────────────────────────┐
│          Servidor Único               │
│  ┌─────────────────────────────────┐  │
│  │  Go Backend + Vue Frontend      │  │
│  │  + MemoryStore                  │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
```

**Limitações:**
- Dados perdidos ao reiniciar
- Limitado a um servidor
- Sem backup automático

### Futuro (Produção)

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLOUD PROVIDER                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐                                               │
│  │ CDN / WAF   │ ◄─── Cloudflare / AWS CloudFront             │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────▼──────┐                                               │
│  │    Load     │ ◄─── AWS ALB / GCP Load Balancer             │
│  │  Balancer   │                                               │
│  └──────┬──────┘                                               │
│         │                                                       │
│  ┌──────▼──────┬──────────────┬──────────────┐                 │
│  │  Backend 1  │  Backend 2   │  Backend N   │ ◄─── K8s/ECS   │
│  └──────┬──────┴──────┬───────┴──────┬───────┘                 │
│         │             │              │                          │
│  ┌──────▼─────────────▼──────────────▼───────┐                 │
│  │            PostgreSQL                      │                 │
│  │         (Primary + Replicas)               │ ◄─── RDS/Cloud │
│  └────────────────────────────────────────────┘      SQL        │
│                                                                 │
│  ┌────────────────────────────────────────────┐                 │
│  │               Redis                         │                 │
│  │         (Sessions + Cache)                  │ ◄─── ElastiCache│
│  └────────────────────────────────────────────┘                 │
│                                                                 │
│  ┌────────────────────────────────────────────┐                 │
│  │               S3 / GCS                      │                 │
│  │         (Arquivos/Mídia)                    │                 │
│  └────────────────────────────────────────────┘                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Migração Planejada

| Componente | Atual | Futuro |
|------------|-------|--------|
| Storage | MemoryStore | PostgreSQL |
| Sessions | In-memory | Redis |
| Arquivos | - | S3/GCS |
| Deploy | Single server | Kubernetes/ECS |
| SSL | Let's Encrypt | Cloudflare/ACM |

---

*Última atualização: Dezembro 2024*

